apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-svmdr-01
  namespace: dest-svmdrapp01
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ontap-nas-svmdr
  resources:
    requests:
        storage: 3Gi
